from tap_kit.streams import Stream
import singer


class ReservationStream(Stream):

    stream = 'reservation'

    meta_fields = dict(

    )

    schema = \
    {
      "properties": {
      }
    }
